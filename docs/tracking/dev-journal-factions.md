# Dev Journal: Faction Tracker 

## Overview

The Faction Tracker is an interactive CLI tool for tabletop RPG game masters. It automates the mechanical bookkeeping of faction turns — tracking stats, assets, Coin, HP, goals, and turn history — so the GM can focus on narrative and decision-making rather than arithmetic.

The mechanics are inspired by the Stars Without Number faction rules, adapted into the GM's own system. Asset and flavor data is fully customizable per campaign.

<br/>

## Core Behaviors

- Load static data (asset definitions, tags, goals) from TOML files at startup
- Load and save campaign state (factions, assets, in-progress turn) from a TOML state file
- Allow the GM to review current faction state, asset lists, and past turn history
- Step through factions one at a time to execute a faction turn
- Accept, validate, and process mechanical actions against the SWN rules
- Update state and record every event to an append-only history log
- Render a human-readable narrative summary of completed turns


## Three Distinct Modes

### Review Mode (read-only)
The GM can browse the current state of the campaign without making any changes:
- View faction summaries (stats, HP, Coin, current goal)
- View asset lists by faction or by world
- Browse past turn history and event logs
- Generate a human-readable narrative of past turns

### Turn Mode (stateful, step-by-step)
The GM executes a new faction turn:
- Factions are ordered randomly at the start of the turn
- The GM steps through each faction one at a time
- For each faction: income is calculated, maintenance is paid, and an action is chosen and processed
- Actions are the mechanical actions defined by SWN rules (see below)
- State is updated after each action; history is recorded
- A turn can be paused mid-execution and resumed later
- At turn end, a narrative summary is generated

### Edit Mode (direct state manipulation)
The GM makes freeform changes to faction state outside of mechanical rules — for campaign setup, world-building, or corrections:
- Edit faction details (name, homeworld, scale, stats, HP, Coin, goal, tags)
- Add, remove, or modify assets directly
- No mechanical validation; changes are applied as entered
- Follows the same form-based wizard pattern as faction create

<br/>

## Mechanical Actions

These are the first-class concepts in the system. Each action encapsulates its own rules, costs, and resolution logic:

- **Abandon Goal** — discard the faction's current goal without completing it
- **Attack** — target a rival's asset with one or more of your own assets
- **Bribe** — spend Coin to influence another faction or gain an advantage
- **Buy Asset** — purchase a new asset on your homeworld or a Base of Influence
- **Expand Influence** — establish a new Base of Influence on a planet
- **Refit Asset** — convert an asset to a different asset of the same type
- **Repair Asset/Faction** — heal HP damage to an asset or the faction itself
- **Sell Asset** — liquidate an asset for half its Coin cost
- **Seize Planet** — attempt to become the ruling government of a world
- **Use Asset Ability** — trigger the special abilities of one or more assets

Change Homeworld is a goal type handled by the Goal Engine, not a discrete action. The faction's homeworld transitions over multiple turns via `CheckLock`; no GM action selection is required.

Actions are designed to be source-agnostic: the same processing engine handles actions input by the GM through the UI or generated by an AI decision-maker in the future.



## AI Decision-Making (Planned)

The tool is designed from the start to support automated faction behavior. The intent is traditional game AI (not LLM-based) that can drive faction decisions during Turn Mode. Two modes are planned:

- **Goal-oriented** — the AI plays optimally toward the faction's current goal
- **In-character** — the AI factors in faction personality and tags when making decisions

The action system's source-agnostic design means the AI and the GM use the same interface. This is not being built now — but every design decision should keep it in mind.

<br/>
<br/>

# Data Architecture

| File | Format | Purpose |
|------|--------|---------|
| `internal/faction/data/*_assets.toml` | TOML | Static asset definitions per category (Force, Cunning, Wealth) |
| `internal/faction/data/tags.toml` | TOML | Static tag definitions |
| `internal/faction/data/goals.toml` | TOML | Static goal definitions |
| `campaigns/<campaignID>/faction_state.toml` | TOML | Current faction state, in-progress turn |
| `campaigns/<campaignID>/history.jsonl` | JSON lines | Append-only event and turn log |

- Static data is read-only at runtime; GMs edit TOML files directly to customize
- The loader globs `*_assets.toml` — new asset category files are auto-discovered
- State is campaign-scoped by ID; multiple campaigns can coexist
- History is append-only; never mutated after writing
- A render function transforms history into a human-readable narrative (`narrate <cycle>` command, `internal/faction/narrative` package)



## Design Principles

- **CLI-first** — core logic is decoupled from any UI layer
- **Source-agnostic actions** — actions can be driven by human input or AI equally
- **Separation of concerns** — review and turn execution are distinct flows
- **Extensible static data** — GMs customize assets and flavor without touching code
- **Robust history** — every state change is recorded; nothing is lost
- **Built to grow** — a TUI or web frontend can be layered on later without touching core logic

<br/>
<br/>

# Open Questions

Design questions that are unresolved and will need answers before the relevant feature can be built.

| # | Question | Relevant Feature |
|---|----------|-----------------|
| 1 | Starting Coin for new factions — no explicit SWN rule; what is the right default (Wealth rating, fixed amount, GM prompt)? | `faction create`, Edit Mode |
| 2 | How does Edit Mode integrate into the Cobra command tree — sub-commands under a top-level `edit` command, or per-entity sub-commands (e.g. `faction edit`)? | Edit Mode |
| 3 | ~~What does the narrative summary renderer output look like — plain text, markdown, something else?~~ Resolved: Markdown — cycle headline (`#`), lede paragraph, per-faction `###` sections, quiet tail | Narrative renderer |
| 4 | When AI decision-making is added, how does the GM designate which factions are AI-driven vs. manually controlled? | AI decision-making |

<br/>
<br/>

# Progress

### Up Next
- TUI Rebuild: fresh Bubbletea TUI against the new `InputCollector` + `TurnObserver` interfaces


### Deferred (minor)
- `BuyAsset` Stealth detection hardcodes `"C3-002"` (`engine/actions/buy_asset.go`): robust fix requires a `"stealth_applicator"` flag in the TOML or a typed `TypeStealth` constant; low risk while asset definitions are static, but silently breaks if the TOML ID changes
- `SeizePlanet` action is invisible in history: `Output()` returns no mutations, so the EventRecord for the turn the seize was initiated has no entry recording the phase transition; a `GoalPhaseAdvanced` mutation would make history replay accurate
- `narrateUseAssetAbility` misleading fallback text: now moot until TUI is rebuilt; will resurface when narration is re-implemented against the new observer interface

### Deferred
- Campaign-scoped static data: move asset definitions, tags, and goals from `internal/faction/data/` into the campaign directory (`campaigns/<campaign-id>/data/`); derive the data path from the campaign ID automatically; `faction-manager campaign create` seeds a starter set of TOML files into the new campaign directory; `FACTION_DATA_DIR` env var removed; no embedded defaults in the binary — GMs own their data entirely. Deferred; no concrete trigger yet.
- Starting Coin: set initial balance (Wealth rating by default); deferred until Coin tracking is designed
- Tag-granted assets: some tags grant bonus starting assets; deferred until engine action resolution is designed
- Command layer does state mutation directly (`s.Factions = append(...)`) and resolves the state file path — both should move into the engine as `engine.CreateFaction(campaignID, faction)` once the engine has more substance. Commands should be thin: collect input, call engine, report result.
- Maintenance costs per asset: `maintenanceCost` returns 0 until structured cost data is added to `AssetDefinition` and resolved via Rulebook
- Programmatic tag handling: v1 does not automate tag mechanics (bonus dice, purchase gates, event hooks, etc.); GMs adjudicate at the table. Design captured in `docs/discovery/tag-engine-discovery.md`
- Tag reminder affordance: surface attacker's and defender's active tags at roll-time in the turn wizard so the GM remembers to apply effects manually; deferred alongside programmatic tag handling

### Completed
- Project scaffolding (`go.mod`, `main.go`, Cobra CLI entrypoint)
- Living design document and decisions log
- Domain types: `AssetDefinition`, `Asset`, `Faction`, `Tag`, `Goal`, `DiceRoll`
- Static data files: all Force, Cunning, and Wealth assets (ratings 1–8), 20 tags, 11 goals
- `Rulebook` loader: globs `*_assets.toml`, parses dice notation, converts to domain types; smoke-tested
- State package: campaign-scoped `faction_state.toml` load/save with auto-dir creation
- Faction commands: `faction delete` (with confirmation dialog), `--campaign` flag wired throughout
- `App` struct with engine initialization; command packages split into `faction/`, `review/`, `turn/`
- `faction create` wizard: name, homeworld, scale, stat assignment, starting assets, tag selection, planetary government prompt, goal selection, confirmation
- Domain helpers (`CalcMaxHP`, `RatingsFromScale`, `AssetCountsFromScale`) moved to `internal/faction/domain`
- `FACTION_DATA_DIR` environment variable for runtime data path resolution
- Internal code review: `FactionScale` exported, `ScaleFromString` removed, `HPValueForRating` switched to switch statement
- TOML tags added to all serialized domain types (`Faction`, `Asset`, `Tag`, `Goal`)
- Tests: `FactionState` TOML round-trip, `parseDice` table-driven, duplicate asset ID detection
- `faction list` command: summary line per faction (name, scale, HP, current goal)
- Turn engine scaffolding: `TurnState` domain type, `TurnEngine` sub-engine, core `Engine` restructured as orchestrator; covers faction ordering, turn state tracking, mid-turn persistence, resume-safe bookkeeping, and step control
- `turn` command: fully interactive wizard with resume/abandon detection, per-faction bookkeeping→action placeholder→commit loop, and Cycle summary; ANSI terminal styling
- `MutationEngine` scaffolding: `Mutation` interface (`Type` only; `Describe` removed), `CoinDelta`/`AssetRemoved`/`AssetMaintainedFlag` concrete types with json tags, `Apply` wired into bookkeeping
- `godotenv` + `config.go` env var management; `FACTION_DATA_DIR` via `.env`; pre-merge code review practice established
- Action Engine foundation: `Action` interface (Validate, Inputs, Resolve, Output), `ActionEngine` with factory-based registration, `InputCollector` interface for swappable GM/AI input collection, `GMCollector` in `cmd/forms` using huh prompts
- `SellAsset` action: first concrete action implementation; full Validate→Inputs→Resolve→Output cycle; mutations wired into `MutationEngine.Apply`
- Turn wizard action phase: replaces placeholder with real action selection, `No Action` option, mutation application
- Engine package reorganized: files renamed to `*_engine.go` convention; `engine.go` → `core.go`
- History Engine: `EventRecord`/`MutationRecord` domain types; `HistoryEngine` appends one per-faction JSONL record per cycle; turn wizard accumulates bookkeeping and action mutations and records them together; skip-turn prompt and press-Enter pause added to turn wizard
- Code review (`chore/code-review-2`): `cmd/faction-manager/paths` package with `paths.New(campaignID)` as canonical campaign path resolver; `domain.FactionStat` typed throughout faction creation wizard; `BookkeepingResult.RecordedMutations` renamed to make already-applied status explicit; `FactionStat` moved to `faction.go`; concrete actions moved to `engine/actions` sub-package
- `simple-actions` branch: four new actions (BuyAsset, RepairAsset, RepairFaction, RefitAsset); new mutation types (`AssetAdded`, `FactionHPDelta`, `AssetHPDelta`); `FactionState.Factions` refactored from slice to map for O(1) lookup by ID; turn-start re-ready of assets purchased the prior cycle (per SWN "inactive until next turn" rule); collision-free asset IDs via monotonic per-(faction, definition) suffix; `InputCollector` extended with `SelectRepairOrders`, `SelectBuyOrder`, `SelectRefitOrder`; `InputCollector` wide-interface shape ratified (revisit refactor after remaining actions land)
- `attack-action` branch: `Base` domain type + `Faction.Bases` slice; homeworld Base seeded at faction create time; `Roller` interface + `RandRoller` production impl + `DiceRoll.Roll(Roller)`; `Attack` action with up-front attacker commit, per-matchup re-check, stealth-cleared dedup, inline destruction, Base-redirect prompt; new mutations (`AssetStealthCleared`, `BaseHPDelta`, `BaseDestroyed`); `InputCollector` extended with `SelectAttackers`, `SelectDefender`, `ConfirmRedirectToBase`; base-redirect on homeworld uses `FactionHPDelta` (non-homeworld Bases land with Expand Influence); comprehensive attack unit tests with fake collector and deterministic roller
- `feature/tui` branch: full-screen Bubbletea TUI replaces the huh wizard for Turn Mode; split-panel layout (faction info left, input right) driven by a nine-state state machine; `TUICollector` bridges pre-collected input to the engine's `InputCollector` interface; all six implemented actions (SellAsset, BuyAsset, RefitAsset, RepairAsset, RepairFaction, Attack) wired with dedicated input sub-models; Attack redirect runs post-roll via goroutine/channel bridge — `ConfirmRedirectToBase` sends `AttackRedirectMsg` to the BubbleTea event loop and blocks until the user answers; per-faction HP/Coin snapshots and action-taken tracking feed a cycle summary table using lipgloss ANSI-aware column widths; history recording (including skipped factions) and state persistence unchanged from wizard; old `wizard.go` and `GMCollector` deleted; action factory registrations use nil collector (validation-only path); `pendingAction` field vestigial — tracked for cleanup
- `feature/tui-qol` branch: play-by-play log panel in the right side of the split layout during `stateActionResult`; narrated post-hoc from mutations + pre-Apply faction state (no engine interface changes); per-action narrators in `tui/narrate.go` cover all six implemented actions; `narrateAttack` derives outcome from mutation deltas + the stored `TUICollector` attacker/defender pairs; `attackCollector *TUICollector` stored across the goroutine boundary in `TurnModel` for this purpose; log resets between factions; `logSummary` feeds a new Result column in the cycle summary table; `FactionSummaryRow.ResultSummary` carries the first log line per faction
- v0.10.1 patch (on `main`, now v0.11.0): three TUI display fixes — left panel now resolves asset names and MaxHP from the `Rulebook` definition instead of showing raw IDs and CurrentHP/CurrentHP; `assetCategoryAbbrev` implemented (F/C/W); `buildSummaryRows` sorted by faction name for deterministic cycle summary order
- `feature/goal-engine` branch: Goal Engine — multi-turn faction objectives with XP rewards; `ActiveGoal` struct replacing `Faction.Goal` pointer; 14 ratified design decisions (see decisions log); 12 goal handlers covering all SWN goal types; `GoalEngine.CheckLock` (pre-turn lock routing) and `GoalEngine.UpdateProgress` (post-action progress inspection); three phases of build: domain foundation, engine core, TUI integration; four new actions (Bribe, Seize Planet, Abandon Goal, BuyAsset Stealth extension); `stateGoalLocked` TUI state for Change Homeworld transit; action menu filtered to Attack-only during Planetary Seizure combat phase; goal narration wired into all action paths via `narrateGoalEvents`; left faction panel shows goal name from rulebook and transit destination/turns for Change Homeworld; all seven new mutation types (`AssetStealthApplied`, `GoalAbandoned`, `GoalCompleted`, `XPAwarded`, `HomeworldChanged`, `TagAdded`, `InfluenceDelta`) with full `MutationEngine.Apply` handlers; attribution fields (`CausedByFactionID`, `Cause`) added to all existing mutation types
- `feature/expand-influence` branch: Expand Influence action — places a new Base of Influence on a world or reinforces an existing one via heal or max-HP sub-modes; `ExpandInfluence` struct in `engine/actions/expand_influence.go` with full Validate→Inputs→Resolve→Output cycle; `resolveNewBase` emits `CoinDelta` before `BaseAdded` (Coin spent even if base is immediately destroyed), then runs a contested roll (1d10+Cunning) per rival on the target world — rivals that tie or beat the faction's roll may make a free attack against the new base; `baseAttack` unexported sub-struct handles rival free attacks, not a registered action; `baseHPTracker *int` threaded by pointer across rival attacks to accumulate pre-Apply damage; three new mutation types (`BaseAdded`, `BaseHealed`, `BaseExpanded`) with full `MutationEngine.Apply` handlers; `InputCollector` extended with `SelectExpandInfluenceOrder`, `ConfirmRivalFreeAttack`, `SelectBaseAttackers`; goroutine/channel bridge extended to two new mid-Resolve prompts — `ConfirmRivalFreeAttack` (bool channel) and `SelectBaseAttackers` (`[]*domain.Asset` channel) — same pattern as Attack redirect; two new TUI states (`stateExpandInfluenceRivalConfirm`, `stateExpandInfluenceSelectAttackers`) and corresponding message types; `inputs/expand_influence.go` four-step wizard with step-skipping when only one option is available; `inputs/base_attackers.go` multi-select list for rival asset selection; `narrateExpandInfluence` narrates from mutations before Apply, building a base-locations map from existing bases plus `BaseAdded` mutations for pre-Apply ID resolution; rivals sorted by name for deterministic processing order; `expand-influence-test` campaign created for manual testing
- `feature/narrative-renderer` branch: narrative renderer — four-phase build producing the `narrate <cycle>` command; `internal/faction/narrative/digest` package with `CycleDigest` types and `digest.Build` — reads `[]domain.EventRecord`, maps all mutation types to structured beat/cross-event data, selects a headline via a five-level priority chain, and splits factions into active and quiet lists; `internal/faction/narrative` package with `Renderer` interface, `NewWireRenderer()` factory, and `wire_renderer.go` deterministic implementation backed by seeded `math/rand` variant pools in `wire_templates.go`; `narrative.LoadCycle` history reader; `narrate/cmd.go` Cobra command with `--campaign` (required), `--seed`, `--out`, `--stdout` flags and increment-safe output path resolution (`cycle-NNN.md` → `cycle-NNN-002.md` → …); `campaigns/narrative-fixture` demo fixture with two factions exercising every digest path; golden-file tests for attack, goal-completed, and quiet renders; determinism and variation unit tests; 11 design decisions ratified (see decisions log #124–134)
- `feature/use-asset-ability` branch: Use Asset Ability action — completes all nine SWN actions; four-phase build across domain types, engine, action, and TUI; `AbilityEngine` in `engine/ability_engine.go` with a two-layer dispatch: step handler registry keyed by `AbilityStepType` for standard step sequences, plus a custom handler override map keyed by asset definition ID for bespoke abilities; two step handlers registered — `movementStepHandler` emits `AssetMoved` (new mutation type) plus an optional `CoinDelta` cost, `factionTestStepHandler` runs a contested d10+stat roll and on attacker win dispatches to `applyAbilityEffect` for `reveal_stealth` / `coin_drain` / `coin_steal`; assets with `Ability == nil` fall through to `ConfirmAbilityApplied` GM adjudication path; `UseAssetAbility` action in `engine/actions/use_asset_ability.go` with Validate→Inputs→Resolve→Output cycle; assets selected up-front as an ordered list (order determines per-asset resolution sequence per SWN rules); `InputCollector` extended with `SelectAbilityAssets`, `SelectMoveDestination`, `SelectFactionTestTarget`, `ConfirmAbilityApplied`; goroutine/channel bridge extended with three new mid-Resolve prompts — `AbilityMoveMsg`, `AbilityFactionTestMsg`, `AbilityConfirmMsg` — each carrying an embedded `ResponseCh`, same pattern as Attack redirect and Expand Influence; three new TUI states (`stateAbilityMoveDestination`, `stateAbilityFactionTestTarget`, `stateAbilityConfirmApplied`); three new input models in `tui/inputs/` (`ability_assets.go` ordered multi-select with resolve-order badges, `move_destination.go` single-select world list, `faction_test_target.go` single-select faction list); `narrateUseAssetAbility` in `tui/narrate.go` derives play-by-play from mutations before Apply; post-review fixes: nil guard after `SelectFactionTestTarget`, nil `EffectDice` guards for coin_drain/coin_steal, faction test candidates sorted by name for deterministic ordering; `use-ability-test` campaign created for manual smoke testing; all nine SWN actions now fully implemented (v0.12.0)
- `feature/testing-suite` branch (Phases 2 & 3): completed the comprehensive test suite; Phase 2 added unit tests for `engine/goal/` (`lock_test.go` covering all four `LockType` outcomes; `progress_test.go` covering all 11 goal progress handlers using synthetic mutation lists) and `engine/ability/` (`ability_test.go` covering move, faction-test, and cancelled-ability paths); Phase 3 restructured the integration test layer — `orchestrator_test.go` rewritten as a white-box unit test for `filterAllowedActions` only (`package engine`), and the three `TestRunCycle_*` integration tests moved into a new `integration_test/` package alongside five new scenarios (`BuyAsset_ReadyNextCycle`, `ExpandInfluence_NewBase`, `ExpandInfluence_Contested`, `GoalCompleted_MilitaryConquest`, `Bribe`); `FixedRoller` promoted to `testharness` package; `checkStep` helper added for labeled assertion output with `go test -v`; `TestRunCycle_Bookkeeping_AssetDestroyedSecondMiss` deferred — blocked on `maintenanceCost` stub; 6 decisions ratified (decisions log #152–157); all packages pass `go test ./internal/faction/...`
- `feature/core-engine-orchestrator` branch: Core Engine Orchestrator — two-phase refactor moving the full per-faction pipeline from the Bubbletea TUI into the engine itself; Phase 1 deleted the TUI and `turn` Cobra command to clear the runway; Phase 2 landed three new interfaces (`InputCollector` extended with `SelectAction` + `AwaitCheckpoint`, `TurnObserver` fire-and-forget observer, `EventHook` interface-only dispatch seam for the future Tag Engine), `internal/faction/config` package, `ActionFactory` signature change to `func(InputCollector) Action`, `Engine.Rand` field for test-injectable dice, `ApplyBookkeeping` refactored to return mutations without applying them, `RunFactionTurn` and `RunCycle` orchestrators on `*Engine`, `RegisterDefaultActions` helper in the actions package, and a headless test harness (`ScriptedCollector`, `RecordingObserver`); Phase 3 extracted all six sub-engines into named sub-packages (`ability`, `action`, `goal`, `history`, `mutation`, `turn`), added `engine.NewWithRulebook` for hermetic tests, renamed `AbilityEngine` field to `Ability`, renamed checkpoint constants from `Phase*` to `Checkpoint*`, and fixed `buildFactionOrder` to use `e.Rand` for deterministic ordering; three load-bearing integration tests assert observer event sequence AND history file contents for a full two-faction cycle, `LockSkip`, and `LockRestrictActions`; 14 design decisions ratified (decisions log #135–148)

<br/>
<br/>

> Full decisions log: [decisions-log.md](decisions-log.md)
